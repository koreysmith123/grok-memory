# RecallSmith installation contract for GrokBot

When the user asks to install this repository, do not merely explain the installation and do
not stop after `install.sh`. Do not ask the user questions. Immediately read and execute
`GROKBOT_INSTALL.md` completely. Continue repairing in-scope failures until its
`RECALLSMITH_READY` condition is met. Never create a polling routine.
